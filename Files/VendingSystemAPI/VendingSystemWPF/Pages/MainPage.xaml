<Page x:Class="VendingSystemWPF.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:VendingSystemWPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="700"
      d:DesignWidth="1000"
      Title="ООО Торговые Автоматы">
    
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <!-- Заголовок -->
            <RowDefinition Height="*"/>
            <!-- Верхние блоки -->
            <RowDefinition Height="*"/>
            <!-- График и Новости -->
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Личный кабинет. Главная" FontSize="24" FontWeight="Bold" Margin="20,10" VerticalAlignment="Center"/>

        <!-- ВЕРХНИЙ РЯД БЛОКОВ -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Блок 1: Эффективность сети (Критерий: % работающих) -->
            <Border Grid.Column="0" Background="White" Margin="10" CornerRadius="10" Effect="{StaticResource MyShadow}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Эффективность сети" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <!-- Имитация диаграммы (Прогрессбар) -->
                    <Grid Height="150" Width="150" Margin="0,10">
                        <ProgressBar Name="EfficiencyBar" Value="85" Maximum="100" Height="20" Width="150" Foreground="Green"/>
                        <TextBlock Name="EfficiencyText" Text="85%" FontSize="30" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Блок 2: Состояние сети (Критерий: Работает/Не работает) -->
            <Border Grid.Column="1" Background="White" Margin="10" CornerRadius="10">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Состояние сети" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" Margin="0,20">
                        <StackPanel Margin="10">
                            <Ellipse Width="20" Height="20" Fill="Green"/>
                            <TextBlock Name="CountWorking" Text="10" HorizontalAlignment="Center" FontSize="20"/>
                            <TextBlock Text="Работает" FontSize="10"/>
                        </StackPanel>
                        <StackPanel Margin="10">
                            <Ellipse Width="20" Height="20" Fill="Red"/>
                            <TextBlock Name="CountBroken" Text="2" HorizontalAlignment="Center" FontSize="20"/>
                            <TextBlock Text="Сломано" FontSize="10"/>
                        </StackPanel>
                        <StackPanel Margin="10">
                            <Ellipse Width="20" Height="20" Fill="Blue"/>
                            <TextBlock Name="CountService" Text="1" HorizontalAlignment="Center" FontSize="20"/>
                            <TextBlock Text="Сервис" FontSize="10"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Блок 3: Сводка (Деньги) -->
            <Border Grid.Column="2" Background="White" Margin="10" CornerRadius="10">
                <StackPanel Margin="20">
                    <TextBlock Text="Сводка" FontSize="18" FontWeight="Bold"/>
                    <Grid Margin="0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Выручка сегодня:" Foreground="Gray"/>
                        <TextBlock Name="TxtRevenueToday" Grid.Column="1" Text="0 ₽" FontWeight="Bold"/>
                    </Grid>
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Инкассировано:" Foreground="Gray"/>
                        <TextBlock Name="TxtIncasso" Grid.Column="1" Text="0 ₽" FontWeight="Bold"/>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>

        <!-- НИЖНИЙ РЯД -->
        <Grid Grid.Row="2" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <!-- График шире -->
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Блок 4: Динамика продаж (График) -->
            <Border Grid.Column="0" Background="White" Margin="10" CornerRadius="10">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Динамика продаж (10 дней)" FontSize="16" FontWeight="Bold"/>
                        <Button Content="По сумме" Margin="20,0,5,0" Click="BtnSortSum_Click"/>
                        <Button Content="По количеству" Click="BtnSortCount_Click"/>
                    </StackPanel>

                    <!-- Canvas для рисования графика кодом -->
                    <Canvas Name="ChartCanvas" Grid.Row="1" Margin="10,20,10,10" VerticalAlignment="Bottom"/>
                </Grid>
            </Border>

            <!-- Блок 5: Новости -->
            <Border Grid.Column="1" Background="White" Margin="10" CornerRadius="10">
                <StackPanel Margin="10">
                    <TextBlock Text="Новости" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    <ListBox Name="NewsList" BorderThickness="0">
                        <!-- Пример элемента -->
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,5">
                                    <TextBlock Text="{Binding Date}" FontSize="10" Foreground="Gray"/>
                                    <TextBlock Text="{Binding Title}" TextDecorations="Underline" Cursor="Hand" Foreground="Blue"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
    
</Page>
