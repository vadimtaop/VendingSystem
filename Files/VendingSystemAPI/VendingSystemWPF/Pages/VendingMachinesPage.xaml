<Page x:Class="VendingSystemWPF.Pages.VendingMachinesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:VendingSystemWPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="600"
      d:DesignWidth="900"
      Title="ООО Торговые Автоматы">

    <Grid Background="#F9F9F9">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" BorderBrush="#EEE" BorderThickness="0,0,0,1" Padding="10">
            <StackPanel Orientation="Horizontal">
                <Button Name="RefreshButton" Content="🔄 Обновить" Margin="5" Click="RefreshButton_Click" Background="#D0EFFF"/>
                <TextBlock Text="Фильтр:" VerticalAlignment="Center" Margin="15,5,5,5"/>
                <TextBox Name="FilterTextBox" Width="200" TextChanged="FilterTextChanged" VerticalAlignment="Center"/>
                <Button Name="AddButton" Content="Добавить (ADO)" Margin="20,0,0,0" Click="AddButtonClick"/>
                
                <!-- КНОПКА ЭКСПОРТА -->
                <Button Name="ExportButton" Content="Экспорт в CSV" Margin="20,0,0,0" Click="ExportButton_Click"/>
                
                <CheckBox Name="ToggleViewCheck" Content="Режим плитки" Margin="20,0,0,0" VerticalAlignment="Center" Checked="ToggleView_Checked" Unchecked="ToggleView_Checked"/>
            </StackPanel>
        </Border>

        <!-- 1. ТАБЛИЦА (DataGrid) - она у тебя уже есть -->
        <DataGrid Grid.Row="1" Name="VendingMachinesDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Компания" Binding="{Binding Company.Title}" Width="120"/>
                <DataGridTextColumn Header="Адрес" Binding="{Binding Address}" Width="150"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status.Title}" Width="100"/>
                <DataGridTemplateColumn Header="Действия" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Удалить (ADO)" Background="Salmon" Click="DeleteButton_Click" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- 2. ПЛИТКИ (ListView) - ВСТАВЬ ЭТОТ БЛОК СРАЗУ ПОСЛЕ DataGrid -->
<ListView Name="VendingMachinesListView" Visibility="Collapsed" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Grid.Row="1">
    <ListView.ItemsPanel>
        <ItemsPanelTemplate>
            <!-- WrapPanel автоматически переносит плитки на новую строку -->
            <WrapPanel Orientation="Horizontal"/>
        </ItemsPanelTemplate>
    </ListView.ItemsPanel>
    <ListView.ItemTemplate>
        <DataTemplate>
            <!-- Здесь мы рисуем одну плитку -->
            <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Margin="10" Padding="10" Width="200" Background="White">
                <StackPanel>
                    <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="Blue" FontSize="14" TextWrapping="Wrap"/>
                    <TextBlock Text="{Binding Address}" Margin="0,5,0,0" Foreground="DarkSlateGray" TextWrapping="Wrap"/>
                    <TextBlock Text="{Binding Model}" Margin="0,5,0,0" FontStyle="Italic"/>
                    <Border Background="LightGreen" CornerRadius="5" Padding="5,2" Margin="0,10,0,0" HorizontalAlignment="Left">
                         <TextBlock Text="{Binding Status.Title}" FontWeight="SemiBold" Foreground="DarkGreen"/>
                    </Border>
                </StackPanel>
            </Border>
        </DataTemplate>
    </ListView.ItemTemplate>
</ListView>

        <!-- ======================================================= -->
        <!-- Панель пагинации и информации (Критерий Б.2)            -->
        <!-- ======================================================= -->
        <Border Grid.Row="2" Background="White" BorderBrush="#EEE" BorderThickness="0,1,0,0" Padding="10">
            <Grid>
                <TextBlock Name="CountTextBlock" Text="Загрузка..." VerticalAlignment="Center" HorizontalAlignment="Left"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Name="TxtPages" Text="Страница 1 из 1" VerticalAlignment="Center" Margin="10,0"/>
                    <Button Name="PrevButton" Content="&lt; Назад" Width="80" Click="BtnPrev_Click"/>
                    <Button Name="NextButton" Content="Вперед &gt;" Width="80" Margin="5,0,0,0" Click="BtnNext_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
    
</Page>
