<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VendingSystemMobile.Pages.LoginPage"
             Title="LoginPage">
    <VerticalStackLayout Spacing="20" Padding="30" VerticalOptions="Center">

        <!-- Логотип (Критерий) -->
        <Image Source="dotnet_bot.png" HeightRequest="100" HorizontalOptions="Center" />

        <Label Text="ВХОД В СИСТЕМУ" FontSize="24" FontAttributes="Bold" HorizontalOptions="Center"/>

        <Entry x:Name="LoginEntry" Placeholder="Логин" />
        <Entry x:Name="PasswordEntry" Placeholder="Пароль" IsPassword="True" />

        <Button Text="Войти" Clicked="OnLoginClicked" BackgroundColor="Blue" TextColor="White"/>
    </VerticalStackLayout>
</ContentPage>