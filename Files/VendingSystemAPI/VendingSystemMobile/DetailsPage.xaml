<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VendingSystemMobile.DetailsPage"
             Title="DetailsPage">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">

            <!-- Инфо -->
            <Label x:Name="LblAddress" FontSize="20" FontAttributes="Bold"/>
            <Label x:Name="LblDesc" FontSize="16"/>

            <!-- Кнопки статусов (0.2 балла) -->
            <HorizontalStackLayout Spacing="10">
                <Button Text="Принять" BackgroundColor="Green" Clicked="Accept_Clicked"/>
                <Button Text="Отклонить" BackgroundColor="Red"/>
            </HorizontalStackLayout>

            <!-- Протокол (Чек-лист) (0.5 балла) -->
            <Label Text="Протокол работ:" FontSize="18" Margin="0,20,0,0"/>

            <CheckBox />
            <Label Text="Осмотр проведен"/>
            <CheckBox />
            <Label Text="Чистка выполнена"/>

            <Entry Placeholder="Заметки мастера..." />

            <!-- Фото (0.2 балла) -->
            <Button Text="📷 Сделать фото" Clicked="Photo_Clicked"/>
            <Image x:Name="PhotoPreview" HeightRequest="200"/>

            <Button Text="ЗАВЕРШИТЬ РАБОТУ" Clicked="Save_Clicked" Margin="0,20,0,0"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>